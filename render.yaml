# Render Blueprint configuration
services:
  - type: web
    name: vector-deploy-create-simple-django
    env: node
    plan: starter
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
